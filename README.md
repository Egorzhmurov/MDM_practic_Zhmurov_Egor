# MDM Control Project

Цей проєкт призначений для дистанційного керування Android-пристроєм через сервер на Python та клієнтську частину (Device Owner) на Android.

## 🛠 Вимоги до системи

* **Python:** версія 3.13.7
* **Android Studio:** остання стабільна версія
* **Інструменти:** Android SDK (платформні інструменти з `adb`)

## 🚀 Налаштування та запуск

### 1. Підготовка сервера (Python)

1.  Склонуйте репозиторій у ваше середовище розробки:

    ```bash
    git clone [посилання на репозиторій]
    ```

2.  Відкрийте папку **MDM-app**.

3.  **Важливо:** Перед запуском налаштуйте шлях до ADB.
    * Відкрийте PowerShell і виконайте команду:

    ```powershell
    Get-ChildItem -Path C:\ -Filter adb.exe -Recurse -ErrorAction SilentlyContinue
    ```

    * Скопіюйте отриманий шлях.

4.  У файлі `mdm-server.py` знайдіть змінну `ADB_PATH` і вставте туди свій шлях.

5.  Запустіть файл `mdm-server.py` (натисніть зелений трикутник або Run через ПКМ).

6.  Переконайтеся, що в терміналі з'явилося повідомлення: `"Очікування зʼєднання на порту 8080"`.

### 2. Налаштування клієнта (Android Studio)

1.  Відкрийте Android Studio.
2.  Перейдіть у меню: **Menu** (три смужки) -> **Open**.
3.  Оберіть папку **MDM_APP_DEVICE_OWNER** та натисніть **Select Folder**.
4.  У структурі проєкту перейдіть до `app` -> `MainActivity.kt` (відкрийте подвійним кліком).
5.  У верхньому правому куті оберіть конфігурацію **MainActivity** (без дужок) та натисніть зелений трикутник для запуску.
6.  Натисніть на іконку смартфона праворуч, щоб відкрити Емулятор.

## 📱 Взаємодія з пристроєм

1.  Коли додаток відкриється в емуляторі, натисніть кнопку **REBOOT DEVICE**. Це ініціює з'єднання з Python-сервером.
2.  Після успішного з'єднання ви зможете вводити команди керування безпосередньо в терміналі вашої IDE (PyCharm тощо).
